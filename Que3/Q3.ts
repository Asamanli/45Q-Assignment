let person : string =  "Saman";

console.log(person.toLowerCase());
console.log(person.toUpperCase());
console.log(person.charAt(0).toUpperCase() + person.slice(1).toLowerCase());
//The .charAt(0) method is used to extract the character at the specified index (in this case, index 0) from the string stored in the variable person.